<Window x:Class="MASA.RetailPOS.App.Views.SplashView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MASA Retail POS Pro" Height="400" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    
    <Border CornerRadius="15" Background="#1E1E2F" BorderThickness="1" BorderBrush="#3A3A5A">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="5" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="20" />
            </Grid.RowDefinitions>

            <!-- Logo Area -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="100" Height="100" CornerRadius="50" Background="#8A2BE2" Margin="0,0,0,20">
                    <TextBlock Text="🛍️" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="MASA Retail POS" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" />
                <TextBlock Text="النسخة الاحترافية الشاملة" FontSize="16" Foreground="#B0B0B0" HorizontalAlignment="Center" Margin="0,5,0,0" />
            </StackPanel>

            <!-- Progress Text -->
            <TextBlock x:Name="StatusText" Grid.Row="1" Text="جاري تحميل النظام..." FontSize="14" Foreground="#D0D0D0" HorizontalAlignment="Center" Margin="0,0,0,10" />

            <!-- Progress Bar -->
            <ProgressBar x:Name="ProgressBar" Grid.Row="2" Height="6" Width="400" Minimum="0" Maximum="100" Value="0" 
                         Foreground="#8A2BE2" Background="#2A2A4A" BorderThickness="0" Margin="0,0,0,20" />
            
            <TextBlock Grid.Row="3" Text="© 2026 MASA Systems. All rights reserved." FontSize="10" Foreground="#555555" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10" />
        </Grid>
    </Border>
</Window>
