<UserControl x:Class="MASA.RetailPOS.App.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <!-- Two main states: List View and Edit View. We use a DataTrigger or just collapse based on IsEditing -->
        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header & Actions -->
            <Border Grid.Row="0" Background="#1E1E2F" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PackageVariant" Width="32" Height="32" Foreground="#8C52FF" VerticalAlignment="Center"/>
                        <TextBlock Text="إدارة المنتجات" FontSize="24" FontWeight="Bold" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" Command="{Binding AddNewProductCommand}" Background="#8C52FF" BorderBrush="#8C52FF" Foreground="White" Height="40" Padding="20,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Width="20" Height="20" Margin="0,0,10,0"/>
                            <TextBlock Text="إضافة منتج جديد" VerticalAlignment="Center" FontSize="16"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Data Grid -->
            <Border Grid.Row="1" Background="#1E1E2F" CornerRadius="10" Padding="15">
                <DataGrid ItemsSource="{Binding Products}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" 
                          BorderBrush="#2D2D42" RowBackground="Transparent" AlternatingRowBackground="#252536" 
                          Foreground="White" ColumnHeaderHeight="50" RowHeight="50">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="150" Foreground="#A0A0B0"/>
                        <DataGridTextColumn Header="اسم المنتج" Binding="{Binding Name}" Width="*" Foreground="White" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="التصنيف" Binding="{Binding Category.Name}" Width="150" Foreground="#A0A0B0"/>
                        <DataGridTextColumn Header="سعر الشراء" Binding="{Binding PurchasePrice, StringFormat={}{0:N2}}" Width="120" Foreground="#A0A0B0"/>
                        <DataGridTextColumn Header="سعر البيع" Binding="{Binding SalePrice, StringFormat={}{0:N2}}" Width="120" Foreground="#4CAF50" FontWeight="Bold"/>
                        <DataGridTextColumn Header="المخزون" Binding="{Binding StockQuantity}" Width="100" Foreground="#A0A0B0"/>
                        
                        <DataGridTemplateColumn Header="إجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="#2196F3" ToolTip="تعديل">
                                            <materialDesign:PackIcon Kind="PencilOutline" />
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="#FF5252" ToolTip="حذف">
                                            <materialDesign:PackIcon Kind="TrashCanOutline" />
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Edit / Add Form -->
        <Border Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#1E1E2F" CornerRadius="10" Padding="30" MaxWidth="800" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="تفاصيل المنتج" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" Text="{Binding SelectedProduct.Name}" materialDesign:HintAssist.Hint="اسم المنتج" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Foreground="White"/>
                    <TextBox Grid.Column="2" Text="{Binding SelectedProduct.Barcode}" materialDesign:HintAssist.Hint="الباركود / رمز المنتج" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Foreground="White"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedProduct.CategoryId}" materialDesign:HintAssist.Hint="التصنيف" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="16" Foreground="White"/>
                    <TextBox Grid.Column="2" Text="{Binding SelectedProduct.StockQuantity}" materialDesign:HintAssist.Hint="الكمية المتوفرة (المخزون)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Foreground="White"/>
                </Grid>

                <Grid Margin="0,0,0,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="{Binding SelectedProduct.PurchasePrice}" materialDesign:HintAssist.Hint="سعر الشراء" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Foreground="White"/>
                    <TextBox Grid.Column="2" Text="{Binding SelectedProduct.SalePrice}" materialDesign:HintAssist.Hint="سعر البيع" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Foreground="White"/>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="إلغاء" Command="{Binding CancelEditCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#A0A0B0" BorderBrush="#A0A0B0" Width="120" Height="40" Margin="0,0,15,0"/>
                    <Button Content="حفظ البيانات" Command="{Binding SaveProductCommand}" Background="#8C52FF" BorderBrush="#8C52FF" Foreground="White" Width="150" Height="40"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
