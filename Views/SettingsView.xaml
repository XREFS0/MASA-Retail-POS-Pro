<UserControl x:Class="MASA.RetailPOS.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <TabControl Background="Transparent" BorderThickness="0">
        <TabItem Header="الأمان والنظام" Background="Transparent" Foreground="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="CogOutline" Width="32" Height="32" Foreground="#8C52FF" VerticalAlignment="Center"/>
                        <TextBlock Text="الإعدادات المتقدمة والأمان" FontSize="24" FontWeight="Bold" Margin="15,0,0,0" VerticalAlignment="Center" Foreground="White"/>
                    </StackPanel>

                    <!-- Top Cards -->
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Change Password -->
                        <Border Grid.Column="0" Background="#1E1E2F" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="تغيير كلمة المرور" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                
                                <PasswordBox x:Name="CurrentPasswordBox" materialDesign:HintAssist.Hint="كلمة المرور الحالية" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,15" Foreground="White" PasswordChanged="CurrentPasswordBox_PasswordChanged"/>
                                <PasswordBox x:Name="NewPasswordBox" materialDesign:HintAssist.Hint="كلمة المرور الجديدة" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,15" Foreground="White" PasswordChanged="NewPasswordBox_PasswordChanged"/>
                                <PasswordBox x:Name="ConfirmPasswordBox" materialDesign:HintAssist.Hint="تأكيد كلمة المرور الجديدة" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,20" Foreground="White" PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                                
                                <Button Content="حفظ التغييرات" Command="{Binding ChangePasswordCommand}" Style="{StaticResource MaterialDesignFlatButton}" Background="#8C52FF" Foreground="White"/>
                            </StackPanel>
                        </Border>

                        <!-- Backup & Restore -->
                        <Border Grid.Column="1" Background="#1E1E2F" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                            <StackPanel>
                                <TextBlock Text="النسخ الاحتياطي لقاعدة البيانات" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                <TextBlock Text="يتم أخذ نسخة احتياطية تلقائياً كل يوم عند تشغيل التطبيق في مجلد Backups." Foreground="#A0A0B0" TextWrapping="Wrap" Margin="0,0,0,20"/>
                                
                                <Button Content="أخذ نسخة احتياطية يدوياً (Backup)" Command="{Binding BackupDatabaseCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#4CAF50" BorderBrush="#4CAF50" Margin="0,0,0,10">
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="DatabaseExport" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                                
                                <Button Content="استعادة من نسخة سابقة (Restore)" Command="{Binding RestoreDatabaseCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#FF5252" BorderBrush="#FF5252">
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="DatabaseImport" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Audit Log -->
                    <Border Grid.Row="2" Background="#1E1E2F" CornerRadius="10" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="سجل نشاطات النظام (Audit Log)" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                            
                            <DataGrid Grid.Row="1" ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False" 
                                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                                      Background="Transparent" RowBackground="#151521" AlternatingRowBackground="#1A1A24"
                                      Foreground="White" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#2D2D42">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="التاريخ والوقت" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="Auto"/>
                                    <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="Auto"/>
                                    <DataGridTextColumn Header="نوع العملية" Binding="{Binding Action}" Width="Auto"/>
                                    <DataGridTextColumn Header="التفاصيل" Binding="{Binding Details}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>
        </TabItem>
        
        <TabItem Header="إدارة المستخدمين" Background="Transparent" Foreground="White">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#1E1E2F" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="قائمة المستخدمين" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="إضافة مستخدم جديد" Command="{Binding AddNewUserCommand}" Style="{StaticResource MaterialDesignFlatButton}" Background="#8C52FF" Foreground="White"/>
                        </Grid>
                        
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                                  Background="Transparent" RowBackground="#151521" AlternatingRowBackground="#1A1A24"
                                  Foreground="White" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#2D2D42">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الاسم الكامل" Binding="{Binding FullName}" Width="*"/>
                                <DataGridTextColumn Header="اسم المستخدم" Binding="{Binding Username}" Width="Auto"/>
                                <DataGridTextColumn Header="الصلاحية" Binding="{Binding Role}" Width="Auto"/>
                                <DataGridCheckBoxColumn Header="مفعل" Binding="{Binding IsActive}" Width="Auto"/>
                                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="تعديل" Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                    <materialDesign:PackIcon Kind="Pencil" Foreground="#2196F3"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="حذف" Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                    <materialDesign:PackIcon Kind="TrashCan" Foreground="#FF5252"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Edit User Form -->
                <Border Grid.Column="1" Background="#1E1E2F" CornerRadius="10" Padding="20" Margin="10,0,0,0" Visibility="{Binding IsEditingUser, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="بيانات المستخدم" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                        
                        <TextBox materialDesign:HintAssist.Hint="الاسم الكامل" Text="{Binding SelectedUser.FullName}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15" Foreground="White"/>
                        <TextBox materialDesign:HintAssist.Hint="اسم الدخول" Text="{Binding SelectedUser.Username}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15" Foreground="White"/>
                        <TextBox materialDesign:HintAssist.Hint="كلمة المرور (اتركه فارغاً لعدم التغيير)" Text="{Binding SelectedUser.PasswordHash}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,15" Foreground="White"/>
                        
                        <ComboBox materialDesign:HintAssist.Hint="الصلاحية" SelectedValue="{Binding SelectedUser.Role}" SelectedValuePath="Content" Margin="0,0,0,15" Foreground="White">
                            <ComboBoxItem Content="Admin"/>
                            <ComboBoxItem Content="Manager"/>
                            <ComboBoxItem Content="Cashier"/>
                            <ComboBoxItem Content="Warehouse"/>
                        </ComboBox>

                        <CheckBox Content="حساب نشط" IsChecked="{Binding SelectedUser.IsActive}" Margin="0,0,0,20" Foreground="White"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="إلغاء" Command="{Binding CancelEditUserCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#A0A0B0" BorderBrush="#2D2D42" Margin="0,0,10,0"/>
                            <Button Content="حفظ" Command="{Binding SaveUserCommand}" Style="{StaticResource MaterialDesignFlatButton}" Background="#8C52FF" Foreground="White"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </TabItem>

        <TabItem Header="تصفير وإدارة البيانات" Background="Transparent" Foreground="White" Visibility="{Binding CanAccessResetTab, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="20">
                <Border Background="#1E1E2F" CornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <materialDesign:PackIcon Kind="DatabaseRemove" Width="32" Height="32" Foreground="#FF5252" VerticalAlignment="Center"/>
                            <TextBlock Text="تصفير النظام وإعادة ضبط المصنع" FontSize="22" FontWeight="Bold" Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Text="تحذير: سيتم حذف البيانات بشكل نهائي وسيتم تصفير الأرقام المتسلسلة (لتبدأ من 1 مجدداً). هذا الإجراء لا يمكن التراجع عنه!" Foreground="#FFB74D" TextWrapping="Wrap" Margin="0,0,0,20" FontSize="14"/>

                        <CheckBox Content="مسح كل الفواتير والمبيعات المعلقة والمكتملة" IsChecked="{Binding ClearInvoices}" Margin="0,5" Foreground="White" FontSize="16"/>
                        <CheckBox Content="مسح كل المنتجات والتصنيفات والمخزون" IsChecked="{Binding ClearProducts}" Margin="0,5" Foreground="White" FontSize="16"/>
                        <CheckBox Content="مسح كل العملاء" IsChecked="{Binding ClearCustomers}" Margin="0,5" Foreground="White" FontSize="16"/>
                        <CheckBox Content="مسح كل الموردين" IsChecked="{Binding ClearSuppliers}" Margin="0,5" Foreground="White" FontSize="16"/>

                        <Button Content="تأكيد وتنفيذ عملية المسح" Command="{Binding ResetDataCommand}" Background="#FF5252" BorderBrush="#FF5252" Foreground="White" Height="45" Margin="0,30,0,0" FontSize="16" FontWeight="Bold">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#D32F2F"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
