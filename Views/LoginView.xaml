<UserControl x:Class="MASA.RetailPOS.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="#151521"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">
             
    <Grid>
        <Border Width="400" Height="500" Background="#1E1E2F" CornerRadius="15" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="10" Color="Black" Opacity="0.5"/>
            </Border.Effect>
            
            <StackPanel Margin="40">
                <!-- Logo -->
                <materialDesign:PackIcon Kind="StorefrontOutline" Width="80" Height="80" 
                                         Foreground="#8C52FF" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                                         
                <TextBlock Text="MASA Retail POS" FontSize="24" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="سجل دخولك للمتابعة" FontSize="14" Foreground="#A0A0B0" 
                           HorizontalAlignment="Center" Margin="0,0,0,40"/>
                
                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF5252" TextWrapping="Wrap" 
                           HorizontalAlignment="Center" FontWeight="SemiBold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Form -->
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="اسم المستخدم" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Foreground="White" FontSize="16" Margin="0,0,0,20"/>
                         
                <PasswordBox x:Name="PasswordBox" 
                             materialDesign:HintAssist.Hint="كلمة المرور" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                             Foreground="White" FontSize="16" Margin="0,0,0,40"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
                             
                <Button Content="تسجيل الدخول" Command="{Binding LoginCommand}" 
                        Background="#8C52FF" BorderBrush="#8C52FF" Foreground="White" 
                        Height="50" FontSize="18" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
